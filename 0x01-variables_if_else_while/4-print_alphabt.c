#include <stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int n;

char lowercase = 'a';
while (lowercase <= 'a');
if (lowercase != 'e' != 'q')
{
purchar (lowercase);
}

return (0);
}
